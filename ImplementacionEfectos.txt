In a unity game, I have multiple characters that extend from the class Entity.

SOME of the child classes of Entity also implement the interface IEffectable. This interface implements the method ApplyEffect, which adds an effect to the entity, temporarily modifying it's attributes like damage, speed, maxHealth, etc.

With ApplyEffect, the game will check if the character already has that effect, and if not, add it to a List of effects. If it already has it, it will only increase it's duration.

Effects should be separate from the entity class, being a class that implements the interface IEffect, which contains the method BeginEffect(which applies the changes) and EndEffect(which undoes them).

BeginEffect should have the ability to override the Entities' methods for the duration of the effect. For example, for the effect "FlameWeakness", the method Entity.takeDamage() should increase damage taken by 25% for the duration of the effect.

I want to make it scalable so that I can keep adding and adding effects to my game without modifying my system. How can I achieve this? I want 